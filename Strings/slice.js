// slice will extract parts of a string and returns the extracted parts of a string - takes start and end indexes to slice

const slice = (str) => {
    return str.slice(0,2);
}

console.log(slice("MMK VARMA COOL"))